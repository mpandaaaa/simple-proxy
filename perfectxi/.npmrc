# The Expo template pulls a web-only react-dom whose peer range is ahead of
# the pinned react. It never ships in the iOS bundle. Without this, a plain
# npm install fails on a conflict that does not affect the app.
legacy-peer-deps=true
